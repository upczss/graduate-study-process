
# Lecture 1：计算机视觉导论

> 课程主题：Computer Vision: From 3D Perception to 3D Reconstruction and Beyond

## 1. 什么是计算机视觉？

计算机视觉（Computer Vision）研究如何让计算机从图像、视频和其他传感器数据中获取信息，并理解现实世界。

可以将视觉系统看成一个转换过程：

```
现实世界
  ↓
传感器（相机、深度相机、激光雷达等）
  ↓
图像 / 视频
  ↓
信息提取
  ↓
场景理解与决策
```

计算机视觉主要解决两个问题：

1. **信息提取（Information Extraction）**
    - 提取图像特征
    - 估计深度
    - 恢复三维结构
    - 估计运动和光流
    - 计算相机和物体的位姿
2. **信息解释（Interpretation）**
    - 识别物体
    - 理解场景
    - 识别人类动作
    - 理解事件和环境状态

---

## 2. 为什么现在计算机视觉发展迅速？

近年来 AI 和计算机视觉快速发展的主要原因有三个。

### 2.1 大量数据

大规模数据集为训练视觉模型提供了基础，例如：

- **ImageNet**：用于大规模图像分类
- **ShapeNet**：用于三维物体和形状理解
- 自动驾驶数据集
- RGB-D 和多视角三维数据集

数据量越大，模型通常越有机会学习到更丰富、更具有泛化能力的视觉表示。

### 2.2 更快的硬件

GPU、TPU 和专用视觉芯片的发展，使得复杂模型的训练和推理速度大幅提高。

这使得以下任务变得更加可行：

- 大规模神经网络训练
- 实时目标检测
- 三维重建
- 自动驾驶感知
- 机器人实时定位与导航

### 2.3 更好的算法

重要的算法进步包括：

- 神经网络（Neural Networks）
- 表示学习（Representation Learning）
- 模仿学习（Imitation Learning）
- 强化学习（Reinforcement Learning）
- 深度学习和大规模预训练模型

其中，**表示学习**的核心思想是：让模型自动从数据中学习有用的特征，而不是完全依赖人工设计特征。

---

## 3. 计算机视觉的主要研究方向

CS231A 重点关注三类问题：

### 3.1 空间与几何（Space / Geometry）

研究如何从图像中恢复物体和场景的三维空间信息。

主要任务包括：

- 物体形状恢复
- 深度估计
- 三维场景重建
- 相机模型与相机标定
- 单视图测量
- 多视图几何
- 双目视觉
- 运动恢复结构（Structure from Motion）
- 三维建模

### 3.2 时间与动态（Time / Dynamics）

研究场景和物体随时间发生的变化。

主要任务包括：

- 目标跟踪
- 特征点跟踪
- 光流估计
- 场景流估计
- 运动理解
- 动态场景理解
- 机器人定位与建图

### 3.3 语义理解（Semantics）

研究图像中的物体、场景和行为具有什么含义。

主要任务包括：

- 物体检测
- 物体识别
- 物体姿态估计
- 场景分类
- 语义分割
- 场景解析
- 行为和事件识别

CS231A 相比 CS231N 更关注：

- 三维几何
- 空间结构
- 相机和多视图
- 三维感知与重建

可以简单理解为：

```
CS231A：偏 3D + 几何
CS231N：偏 2D + 语义 + 深度学习
```

---

## 4. CS231A 的核心目标

课程主要研究两个方向：

### 4.1 使用几何方法估计空间属性

目标是从一张或多张图像中恢复：

- 相机的位置和方向
- 物体的三维位置
- 场景深度
- 物体的形状
- 场景的三维结构

基本思想：

```
图像观测 + 相机模型 + 几何约束
                  ↓
             三维世界估计
```

### 4.2 使用学习方法估计语义和动态属性

目标是从图像或视频中预测：

- 物体类别
- 物体位置
- 物体姿态
- 运动状态
- 场景语义
- 未来行为

基本思想：

```
图像 / 视频 + 数据 + 学习模型
                  ↓
             语义与运动理解
```

---

## 5. 相机系统

相机系统的基本任务是建立从三维世界到二维图像的映射：
现实世界中的三维点经过相机投影后，变成图像平面上的二维点。

课程后续会研究：

- 相机模型
- 针孔相机
- 相机内参和外参
- 相机标定
- 焦距估计
- 相机姿态估计
- 单视图测量
- 多视图几何
- 极线几何
- 双目视觉
- 结构恢复

### 5.1 相机标定

相机标定（Camera Calibration）是估计相机参数的过程。

相机参数主要包括：

- **内参（Intrinsic Parameters）**
    
    - 焦距
    - 主点
    - 像素比例
    - 镜头畸变
- **外参（Extrinsic Parameters）**
    
    - 相机在世界坐标系中的位置
    - 相机的旋转方向

### 5.2 单视图测量

单视图测量（Single-view Metrology）尝试仅从一张图像推断三维信息，例如：

- 物体的实际尺寸
- 平行线和消失点
- 场景中的距离关系
- 物体的空间位置

### 5.3 多视图几何

多视图几何（Multiple-view Geometry）利用同一场景的多个视角恢复三维结构。

核心思想：

```
多个二维观测
     ↓
几何约束
     ↓
三维结构与相机运动
```

相关概念包括：

- 极线几何（Epipolar Geometry）
- 双目视觉（Stereo Vision）
- 三角测量（Triangulation）
- 运动恢复结构（Structure from Motion）

---

## 7. 课程内容安排

CS231A 的主要内容大致分为两个阶段。

### 阶段一：几何与三维感知

1. 计算机视觉导论
2. 相机模型
3. 相机标定
4. 单视图测量
5. 极线几何
6. 双目视觉
7. 运动恢复结构
8. 主动立体与体积立体
9. 拟合与匹配

### 阶段二：动态与学习

10. 表示与表示学习
11. 期中考试
12. 深度估计与特征跟踪
13. 光流与场景流
14. 最优估计
15. 最优估计的应用
16. 神经辐射场（NeRF）
17. 高斯泼溅（Gaussian Splatting）
18. 嘉宾讲座

---

## 8. 课程所需基础

学习本课程需要具备以下基础：

- 线性代数
- 概率论
- 统计学
- 机器学习
- 计算机视觉
- 基本编程能力

尤其需要熟悉：

- 矩阵和向量运算
- 特征值与特征向量
- 概率分布和贝叶斯推断
- 优化方法
- 线性滤波
- 边缘检测
- 角点检测
- SVM
- 聚类
- 神经网络

---

## 9. 本讲重点总结

### 核心概念

- 计算机视觉的目标是从视觉数据中提取信息并理解世界。
- 信息提取关注特征、深度、三维结构和运动。
- 信息解释关注物体、场景、动作和事件。
- CS231A 的重点是三维感知、几何和三维重建。
- 计算机视觉问题可以从空间、时间和语义三个维度理解。
- 相机系统建立了三维世界到二维图像的映射。
- 多视图几何通过多个二维观测恢复三维信息。
- 数据、硬件和算法是推动视觉发展的三大因素。
- 几何方法和学习方法是现代计算机视觉的两类重要工具。

### 一句话理解 CS231A

> CS231A 研究如何利用相机图像、几何约束和学习方法，让计算机理解现实世界的三维结构、运动和语义。


下面这版按讲义的结构补全了针孔模型、透镜、数字相机矩阵、内外参、标定、畸变、刚体变换与不同投影模型。公式中的 `$$` 已经以可复制的原始形式显示，直接复制到 Obsidian 即可。

# Lecture 2：相机模型（Camera Models）

## 1. 本讲核心问题

相机是计算机视觉系统观察现实世界的入口。本讲关注的问题是：

> 三维世界中的一个点，如何经过相机成像，变成数字图像中的一个像素？

整个过程可以概括为：

世界坐标系中的三维点 → 相机坐标系中的三维点 → 图像平面坐标 → 像素坐标。

后续的相机标定、单视图测量、双目视觉、三维重建等内容，都是建立在这个模型之上的。

## 2. 针孔相机模型

针孔相机由一个带小孔的遮挡板和一个成像平面组成。三维物体上的每个点都会向各个方向发射光线；小孔只允许其中极少数光线穿过，从而使三维点与成像平面上的点建立对应关系。

如果没有小孔，来自不同三维点的光线会同时到达成像平面的同一区域，图像会模糊。针孔通过限制光线方向来形成清晰图像。

设相机光心位于坐标原点，三维点为：

$$  
P=  
\begin{bmatrix}  
x\  
y\  
z  
\end{bmatrix}  
$$

其中：

- $x$：点在相机坐标系中的水平坐标
- $y$：点在相机坐标系中的垂直坐标
- $z$：点到相机的深度
- $z>0$：通常表示点位于相机前方

设焦距为 $f$，该三维点在图像平面上的投影点为 $P'=(x',y')^T$。根据相似三角形：

$$  
x'=f\frac{x}{z}  
$$
$$  
y'=f\frac{y}{z}  
$$

写成向量形式：
$$  
P'=  
\begin{bmatrix}  
x'\  
y'  
\end{bmatrix}
=
\begin{bmatrix}  
f\frac{x}{z}\  
f\frac{y}{z}  
\end{bmatrix}  
$$

这就是针孔相机模型最重要的投影关系。

这里的关键是除以深度 $z$：

- 当 $z$ 增大时，投影坐标会缩小，远处物体看起来更小。
- 当 $z$ 减小时，投影坐标会变大，近处物体看起来更大。
- 投影后深度信息被压缩，因此仅凭二维像素点通常不能唯一确定原始三维点。

这就是透视投影中的“近大远小”。

## 3. 实像平面与虚像平面

真实针孔相机的成像平面位于小孔后方，因此得到的图像会上下、左右颠倒。

在计算机视觉中，通常将图像平面放到小孔前方，构造一个虚像平面。这样得到的投影图像是正立的，公式也更直观。

真实像平面和虚像平面中的图像只差一个缩放与翻转关系，因此它们在几何分析中是等价的。后续课程一般默认使用虚像平面。

## 4. 光圈大小、清晰度与亮度

针孔模型要求孔径尽可能小，但现实中存在一个矛盾：

- 小孔只允许少量光线通过，图像更清晰，但亮度更低。
- 大孔允许更多光线通过，图像更亮，但来自不同三维点的光线会混合，使图像变模糊。

因此，针孔相机可以清楚地解释成像几何，却不适合作为实际相机的完整物理模型。

## 5. 透镜模型与景深

现代相机使用透镜来解决“图像清晰”和“光线充足”之间的冲突。

透镜具有两个重要性质：

第一，平行于光轴的光线会在焦点处汇聚。

第二，经过透镜中心附近的光线在近似模型下可以认为不发生偏折。

焦点到透镜中心的距离称为焦距 $f$。透镜会使来自特定三维点的多条光线会聚到图像平面上的同一个点，因此可以在更大的孔径下保持清晰。

但是，透镜只能使特定距离附近的物体准确聚焦。距离不同的物体可能无法在图像平面上汇聚为一点，从而产生失焦模糊。

景深（Depth of Field）指能够在图像中保持清晰的深度范围。景深越小，背景虚化通常越明显；景深越大，更多距离范围内的物体会保持清晰。

在薄透镜近似下，投影关系仍然类似针孔模型：


$$  
P'=  
\begin{bmatrix}  
x'\  
y'  
\end{bmatrix}
=
\begin{bmatrix}  
z'\frac{x}{z}\  
z'\frac{y}{z}  
\end{bmatrix}  
$$

其中 $z'$ 是图像平面相对于透镜的位置。针孔模型中通常有 $z'=f$。

## 6. 从理想图像平面到数字像素

针孔模型中的 $x',y'$ 是连续的物理坐标，例如毫米；但数字图像中的位置用离散像素表示。二者之间还存在坐标原点不同的问题。

图像平面的原点通常位于光轴与图像平面的交点；而数字图像的原点通常位于图像左上角或左下角。因此需要一个平移量：

$$  
\begin{bmatrix}  
c_x\  
c_y  
\end{bmatrix}  
$$

其中 $c_x,c_y$ 称为主点（principal point）的像素坐标。

加入主点偏移后：

$$  
x'=f\frac{x}{z}+c_x  
$$

$$  
y'=f\frac{y}{z}+c_y  
$$

此外，水平和垂直方向的像素尺寸可能不同。设：

- $k$：水平方向从物理长度到像素的转换比例
- $l$：垂直方向从物理长度到像素的转换比例

则：

$$  
x'=fk\frac{x}{z}+c_x  
$$

$$  
y'=fl\frac{y}{z}+c_y  
$$

定义：

$$  
\alpha=fk  
$$

$$  
\beta=fl  
$$

最终常用的像素坐标投影公式为：

$$  
x'=\alpha\frac{x}{z}+c_x  
$$

$$  
y'=\beta\frac{y}{z}+c_y  
$$

其中 $\alpha,\beta$ 都是以像素为单位的有效焦距。若 $k=l$，即水平和垂直像素尺度相同，则称相机具有方形像素（square pixels）。

## 7. 齐次坐标（把除法写出矩阵乘法）

投影公式中包含 $x/z$ 和 $y/z$，因此它不是普通的线性变换。为了将投影统一写成矩阵乘法，需要引入齐次坐标。齐次坐标的目标：把除法隐藏起来，全部运算写成矩阵乘法，最后一步再除以尺度因子w变回普通坐标。

普通二维点：

$$  
(x,y)  
$$

对应的齐次坐标：

$$  
(x,y,1)^T  
$$

普通三维点：

$$  
(x,y,z)  
$$

对应的齐次坐标：

$$  
(x,y,z,1)^T  
$$

更一般地，齐次坐标：

$$  
(a,b,w)^T  
$$

对应的普通二维坐标为：

$$  
\left(  
\frac{a}{w},  
\frac{b}{w}  
\right)^T  
$$

齐次坐标的本质是：先通过矩阵计算得到一个带尺度 $w$ 的向量，最后再除以 $w$ 恢复普通坐标。这样就能将平移、旋转和透视投影放入统一的矩阵框架中。
齐次坐标不是唯一的：
![](../图片/Pasted%20image%2020260815003519.png)
![](../图片/Pasted%20image%2020260815003816.png)
![](../图片/Pasted%20image%2020260815004757.png)
## 8. 相机内参矩阵

在齐次坐标下，相机的内部成像属性可以写成内参矩阵：

![](../图片/Pasted%20image%2020260815004843.png)

![](../图片/Pasted%20image%2020260815004931.png)
![](../图片/Pasted%20image%2020260815005035.png)

若三维点已经位于相机坐标系中，则：

$$  
\tilde{p}=K  
\begin{bmatrix}  
I & 0  
\end{bmatrix}  
\tilde{P}  
$$

![](../图片/Pasted%20image%2020260815005359.png)

内参是相机本身的固有属性。更换相机、改变焦距、改变分辨率或裁剪图像，都可能影响内参。

## 9. 像素轴倾斜（Skew）

理想情况下，图像的水平轴和垂直轴夹角为 $90^\circ$。但由于传感器制造误差，两个轴可能略微倾斜。

加入倾斜角 $\theta$ 后，更一般的内参矩阵为：

![](../图片/Pasted%20image%2020260815010133.png)

大多数现代相机的倾斜可以忽略，因此实际使用中经常令 skew 为 $0$，采用更简单的内参矩阵。
![](../图片/Pasted%20image%2020260815010230.png)

![](../图片/Pasted%20image%2020260815010255.png)

## 10. 相机外参

前面的内参矩阵假设三维点已经用相机坐标系表示。但现实中，三维点通常使用世界坐标系表示。

设：

- $P_w$：世界坐标系中的三维点
- $P_c$：相机坐标系中的三维点
- $R$：旋转矩阵
- $T$：平移向量

世界坐标到相机坐标的变换为：

![](../图片/Pasted%20image%2020260815005620.png)

其中 $R$ 是 $3\times3$ 旋转矩阵，描述相机的朝向；$T$ 是 $3\times1$ 向量，描述世界坐标原点相对于相机的位置。

$R,T$ 统称为相机外参（extrinsic parameters），因为它们描述的是相机在外部世界中的姿态，而不是相机自身的光学结构。

## 11. 完整相机矩阵

将世界坐标到相机坐标的变换，与相机内部投影结合起来：

$$  
\tilde{p}=K  
\begin{bmatrix}  
R & T  
\end{bmatrix}  
\tilde{P}_w  
$$

定义完整投影矩阵：

$$  
M=K  
\begin{bmatrix}  
R & T  
\end{bmatrix}  
$$

则：

$$  
\tilde{p}=M\tilde{P}_w  
$$

矩阵 $M$ 的尺寸为 $3\times4$。它一共具有 11 个自由度：

- 内参提供 5 个自由度
- 三维旋转提供 3 个自由度
- 三维平移提供 3 个自由度
![](../图片/Pasted%20image%2020260815010032.png)
整体的尺度不影响投影结果，因此不额外计入一个自由度。

这是本讲最重要的公式之一：

$$  
\boxed{  
\tilde{p}=K  
\begin{bmatrix}  
R & T  
\end{bmatrix}  
\tilde{P}_w  
}  
$$
![](../图片/Pasted%20image%2020260815005947.png)
## 12. 相机标定(反解相机的内参与外参)

相机标定（Camera Calibration）是从图像中估计相机内参和外参的过程。

通常使用尺寸已知的棋盘格标定板。标定板上的角点在世界坐标系中的位置已知，同时可以从拍摄到的图像中找到对应的像素位置。
![](../图片/Pasted%20image%2020260815010525.png)
设第 $i$ 个三维点和图像点为：

$$  
P_i=  
\begin{bmatrix}  
X_i\  
Y_i\  
Z_i\  
1  
\end{bmatrix}  
$$

$$  
p_i=  
\begin{bmatrix}  
u_i\  
v_i  
\end{bmatrix}  
$$

将投影矩阵 $M$ 的三行记为：

$$  
M=  
\begin{bmatrix}  
m_1^T\  
m_2^T\  
m_3^T  
\end{bmatrix}  
$$

则：

$$  
u_i=\frac{m_1^TP_i}{m_3^TP_i}  
$$

$$  
v_i=\frac{m_2^TP_i}{m_3^TP_i}  
$$

移项可得每个点对应关系提供的两个线性约束：

$$  
u_i(m_3^TP_i)-m_1^TP_i=0  
$$

$$  
v_i(m_3^TP_i)-m_2^TP_i=0  
$$

投影矩阵有 11 个自由度，每一组二维—三维点对应关系提供 2 个约束，因此至少需要 6 组不退化的对应点。

实际中通常会使用远多于 6 个角点，因为检测结果会受到噪声影响。此时问题变成超定方程组，需要最小化重投影误差。

可以写成：

$$  
\min_m \lVert Pm\rVert_2^2  
$$

满足：

$$  
\lVert m\rVert_2^2=1  
$$

其中 $m$ 是将矩阵 $M$ 展开后得到的参数向量，$P$ 是由所有点对应关系构成的系数矩阵。

该问题可以通过奇异值分解（SVD）求解。若：

$$  
P=UDV^T  
$$

则 $m$ 取 $V$ 的最后一列，即对应最小奇异值的右奇异向量。

## 13. 标定中的退化情况

并非任意的点对应关系都能正确求出相机矩阵。

例如，如果所有三维点都落在同一个平面上，直接使用一般的相机矩阵求解会出现退化（degenerate configuration）问题，无法唯一恢复全部参数。

这也是为什么相机标定时通常会从多个角度拍摄棋盘格，让角点在三维空间中形成足够丰富的约束。

## 14. 径向畸变

真实镜头并不完全符合理想针孔或薄透镜模型。最常见的误差是径向畸变（radial distortion）：离光轴越远，图像放大倍率越可能变化。

常见畸变包括：

- 桶形畸变（barrel distortion）：图像边缘向外鼓起，鱼眼或广角镜头中很常见。
- 枕形畸变（pincushion distortion）：图像边缘向内收缩。

考虑径向畸变后，投影不再是简单的线性问题，通常需要非线性优化来同时估计相机参数与畸变参数。

一个重要观察是：径向畸变通常不改变点相对于图像中心的方向，因此坐标比值仍满足：


$$  
\frac{u_i}{v_i}

\frac{m_1^TP_i}{m_2^TP_i}  
$$

这能帮助将原本复杂的标定问题拆分为更容易处理的部分。
![](../图片/Pasted%20image%2020260815011101.png)
## 15. 刚体变换与仿射变换

三维视觉中最常见的几何变换包括旋转、平移和缩放。
刚体变换：只做旋转 + 平移，物体形状、尺寸、角度完全不变，没有拉伸、缩放、剪切。相机外参R,T本质就是三维刚体变换。
旋转矩阵 $R$ 满足：

$$  
R^TR=I  
$$

$$  
\det(R)=1  
$$
![](../图片/Pasted%20image%2020260815011255.png)

![](../图片/Pasted%20image%2020260815011315.png)
![](../图片/Pasted%20image%2020260815011321.png)
![](../图片/Pasted%20image%2020260815011335.png)
![](../图片/Pasted%20image%2020260815011353.png)

使用齐次坐标后，旋转和平移可以合并成一次矩阵乘法：


![](../图片/Pasted%20image%2020260815011433.png)

## 16. 弱透视投影与正交投影

透视投影最真实，但公式中包含除以深度 $z$，计算较复杂。

当物体的深度变化很小，并且物体距离相机较远时，可以用平均深度 $z_0$ 近似所有点的深度：

$$  
z\approx z_0  
$$

此时透视投影近似为：

$$  
x'=\frac{f'}{z_0}x  
$$

$$  
y'=\frac{f'}{z_0}y  
$$

这称为弱透视投影（weak perspective projection）。它相当于对三维物体进行固定倍数的缩放，深度造成的非线性基本消失。

进一步忽略缩放比例，可以得到正交投影（orthographic projection）：

$$  
x'=x  
$$

$$  
y'=y  
$$

正交投影完全忽略深度，投影光线彼此平行。它常用于建筑图纸、工业设计和深度变化较小的近似场景。

## 17. 本讲总结

本讲从针孔相机出发，建立了完整的三维到二维投影模型。

最基础的透视投影是：

$$  
x'=f\frac{x}{z},  
\qquad  
y'=f\frac{y}{z}  
$$

考虑数字图像像素坐标后：

$$  
x'=\alpha\frac{x}{z}+c_x,  
\qquad  
y'=\beta\frac{y}{z}+c_y  
$$

完整的世界坐标到图像坐标映射为：

$$  
\tilde{p}=K  
\begin{bmatrix}  
R & T  
\end{bmatrix}  
\tilde{P}_w  
$$

其中，$K$ 是内参矩阵，描述相机如何成像；$R,T$ 是外参，描述相机相对于世界的位置和方向。

相机标定的目标就是通过已知三维点和图像点的对应关系，估计这些参数，并进一步校正镜头畸变。

# Lecture 3：相机模型 II 与相机标定

## 1. 本讲目标

上一讲已经建立了从三维世界到二维图像的投影模型：

$$  
\tilde{p}=K  
\begin{bmatrix}  
R & T  
\end{bmatrix}  
\tilde{P}_w  
$$

这一讲要解决的问题是：

> 当我们不知道相机参数时，能否仅通过拍摄已知结构的标定板，反推出相机的内参和外参？

本讲包括四部分：

1. 回顾透视、弱透视和正交投影模型。
2. 用二维—三维点对应关系估计相机矩阵。
3. 使用 SVD 求解线性标定问题。
4. 考虑真实镜头的径向畸变，并用非线性优化进一步校正。

## 2. 完整的相机投影模型

设世界坐标系中的三维点为：

$$  
\tilde{P}_w=  
\begin{bmatrix}  
X_w\  
Y_w\  
Z_w\  
1  
\end{bmatrix}  
$$

图像中的二维点写成齐次坐标：

$$  
\tilde{p}=  
\begin{bmatrix}  
u\  
v\  
1  
\end{bmatrix}  
$$

完整的投影关系是：

![](../图片/Pasted%20image%2020260815002415.png)

其中：

- $K$：相机内参矩阵，描述焦距、主点和像素坐标轴关系。
- $R$：旋转矩阵，描述相机朝向。
- $T$：平移向量，描述相机位置。
- $M$：$3\times4$ 的完整相机投影矩阵。
- $\tilde{P}_w$：世界坐标系中的三维点。
- $\tilde{p}$：图像中的二维齐次坐标。

将 $M$ 的三行写为：
![](../图片/Pasted%20image%2020260815012756.png)

这里分母 $m_3^T\tilde{P}_w$ 就是齐次坐标归一化时的尺度项，也对应透视投影中的深度相关量。

## 3. 三种投影模型

### 3.1 透视投影

透视投影是最真实的模型：

$$  
x'=f\frac{x}{z}  
$$

$$  
y'=f\frac{y}{z}  
$$

其中 $f$ 是焦距，$z$ 是三维点到相机的深度。

因为有除以 $z$ 的操作，所以远处物体会缩小，平行线可能在图像中相交。这种模型用于三维重建、Structure from Motion 和 SLAM 等需要精确几何关系的任务。

### 3.2 弱透视投影

若物体本身的深度变化很小，并且整体距离相机较远，可以把所有点的深度近似为平均深度 $z_0$：

$$  
z\approx z_0  
$$

于是透视投影变为：

$$  
x'\approx\frac{f}{z_0}x  
$$

$$  
y'\approx\frac{f}{z_0}y  
$$

定义放大倍率：

$$  
s=\frac{f}{z_0}  
$$

则：

$$  
x'=sx  
$$

$$  
y'=sy  
$$

弱透视模型把原本与深度有关的非线性投影，简化为固定比例的缩放。它适合小且远的物体，在目标识别中较常用。

### 3.3 正交投影

正交投影进一步忽略缩放和深度：

$$  
x'=x  
$$

$$  
y'=y  
$$

此时投影光线彼此平行，深度完全不影响图像位置。它不适合真实相机成像，但在工业设计、建筑制图和某些近似场景中很有用。

## 4. 相机标定问题

相机标定的目标是估计：

$$  
K,\ R,\ T  
$$

也就是相机的内参和外参。

标准做法是使用一个几何尺寸已知的标定物，例如棋盘格。棋盘格角点的三维坐标已知，同时可以在图像中检测到这些角点的二维像素坐标。

设有 $n$ 个对应点：

$$  
\tilde{P}_1,\tilde{P}_2,\ldots,\tilde{P}_n  
$$

以及对应的图像点：

$$  
p_1,p_2,\ldots,p_n  
$$

每一组对应关系都提供关于相机矩阵 $M$ 的约束。
![](../图片/Pasted%20image%2020260815014126.png)
## 5. 为什么至少需要 6 个点？

投影矩阵 $M$ 是一个 $3\times4$ 矩阵，一共有 12 个元素：

$$  
M=  
\begin{bmatrix}  
m_{11} & m_{12} & m_{13} & m_{14}\  
m_{21} & m_{22} & m_{23} & m_{24}\  
m_{31} & m_{32} & m_{33} & m_{34}  
\end{bmatrix}  
$$

但 $M$ 乘以任意非零常数后，投影结果不变。因此它只有 11 个自由度。

一个三维点对应一个二维像素点，可以提供两个独立方程。因此：

$$  
2n\geq 11  
$$

至少需要：

$$  
n=6  
$$

组二维—三维点对应关系。

实际中通常会使用更多的角点，因为图像中的角点检测和标定板的位置都会存在噪声。更多的点可以让结果更稳定。

## 6. 从点对应关系构造线性方程
又叫DLT 直接线性变换（Direct Linear Transform），作用：用多组 3D‑2D 点对应，**线性求解投影矩阵 M**，不迭代、不需要初值，是相机标定 / PnP 的基础。

第 $i$ 个三维点的像素坐标为：

$$  
p_i=  
\begin{bmatrix}  
u_i\  
v_i  
\end{bmatrix}  
$$

根据投影关系：

$$  
u_i=\frac{m_1^T\tilde{P}_i}{m_3^T\tilde{P}_i}  
$$

$$  
v_i=\frac{m_2^T\tilde{P}_i}{m_3^T\tilde{P}_i}  
$$

将分母移到左侧：

$$  
u_i(m_3^T\tilde{P}_i)-m_1^T\tilde{P}_i=0  
$$

$$  
v_i(m_3^T\tilde{P}_i)-m_2^T\tilde{P}_i=0  
$$

这两个公式是直接线性变换（DLT, Direct Linear Transform）的核心。

把所有 $n$ 个点的方程叠加，可以写成：

$$  
Pm=0  
$$
![](../图片/Pasted%20image%2020260815014401.png)

![](../图片/Pasted%20image%2020260815014450.png)
## 7. 用 SVD 求解相机矩阵

![](../图片/Pasted%20image%2020260815014511.png)

总是存在平凡解：

$$  
m=0  
$$

但零矩阵不能表示相机，因此需要寻找一个非零的 $m$，使得 $Pm$ 尽可能接近零。
![](../图片/Pasted%20image%2020260815015727.png)

对矩阵 $P$ 进行奇异值分解：

$$  
P=UDV^T  
$$

其中：
![](../图片/Pasted%20image%2020260815015739.png)

对应最小奇异值的右奇异向量，就是使 $\lVert Pm\rVert$ 最小的解。因此，$m$ 取 $V$ 的最后一列。

得到 $m$ 后，将其重新排列为 $3\times4$ 矩阵，即得到估计的相机矩阵 $M$。

这个线性解通常只是初始结果，因为真实数据中仍然存在噪声和镜头畸变。

## 8. 从 $M$ 分解内参与外参

根据相机模型：

$$  
M=K  
\begin{bmatrix}  
R&T  
\end{bmatrix}  
$$

将 $M$ 按列拆分：

$$  
M=  
\begin{bmatrix}  
A&b  
\end{bmatrix}  
$$

其中：

- $A$ 是 $3\times3$ 矩阵。
- $b$ 是 $3\times1$ 向量。

因为：

$$  
A=KR  
$$

$$  
b=KT  
$$

所以可以从 $A$ 中恢复内参 $K$ 和旋转 $R$，再利用 $b$ 求得平移 $T$。

![](../图片/Pasted%20image%2020260815020112.png)

需要注意：线性标定得到的 $M$ 只确定到一个整体尺度，因此分解时还需要处理尺度符号，并确保旋转矩阵满足：

$$  
R^TR=I  
$$

$$  
\det(R)=1  
$$

## 9. 退化情况

不是任意点集都可以用于标定。

如果所有三维点都位于同一个平面上，普通 DLT 标定会出现退化。原因是平面点无法提供足够的三维深度约束，导致一些相机参数不能被唯一确定。

讲义中还指出：如果所有点位于两个二次曲面的交线上，也可能出现退化。

实践中应让标定板在不同位置、不同方向被拍摄，使观察到的角点尽可能提供丰富的空间约束。

## 10. 径向畸变

前面的模型假设镜头是理想的，但真实镜头常有径向畸变（Radial Distortion）。

径向畸变的特点是：离光轴越远，图像放大倍率变化越明显。其原因是光线穿过镜头边缘时，折射行为更难满足理想模型。

常见的畸变包括：

- 桶形畸变：图像边缘向外鼓起，广角和鱼眼镜头中很常见。
- 枕形畸变：图像边缘向内收缩。
![](../图片/Pasted%20image%2020260815020241.png)
设像素点到畸变中心的距离为：

$$  
d=\sqrt{u^2+v^2}  
$$

可以用多项式来描述畸变系数：

$$  
\lambda(d)=a+bd^2+cd^4+\cdots  
$$

其中 $a,b,c$ 是畸变参数。

畸变后的投影可以写成：

$$  
p_i=Q_{\lambda}M\tilde{P}_i  
$$

![](../图片/Pasted%20image%2020260815020323.png)

因为 $\lambda$ 本身也是未知量，这个问题不再是线性方程组，不能直接只用 SVD 完成求解。
![](../图片/Pasted%20image%2020260815020348.png)
## 11. 为什么径向畸变仍有可利用的信息？

径向畸变会改变点到图像中心的距离，但通常不会改变点相对于中心的方向，也就是不会改变斜率。

因此：

$$  
\frac{u_i}{v_i}

\frac{m_1^T\tilde{P}_i}{m_2^T\tilde{P}_i}  
$$

移项后：

$$  
v_i(m_1^T\tilde{P}_i)-u_i(m_2^T\tilde{P}_i)=0  
$$

这个关系与 $m_3$ 和径向畸变参数无关，因此可以先通过 SVD 估计 $m_1,m_2$。

之后再将 $m_3$ 与畸变参数 $\lambda$ 放入非线性优化中求解。这是一种“先线性估计，再非线性精化”的思路。

## 12. 非线性标定与重投影误差

加入畸变后，标定问题可写为：

$$  
X=f(Q)  
$$

其中：

- $X$：观测到的二维像素点。
- $Q$：待估计的参数，包括内参、外参和畸变参数。
- $f(\cdot)$：从三维点到二维像素点的非线性投影函数。

线性 SVD‑DLT 已经不能求解，使用迭代非线性优化：牛顿法、LM（Levenberg‑Marquardt）算法，视觉标定、SLAM 里最常用 LM。。

LM 算法在视觉中的应用非常广泛，因为它通过迭代最小化重投影误差：

$$  
\sum_i  
\left|  
p_i-\hat{p}_i(Q)  
\right|_2^2  
$$

其中：

- $p_i$：真实检测到的像素位置。
- $\hat{p}_i(Q)$：当前相机参数预测得到的像素位置。
- 两者的差称为重投影误差（reprojection error）单位是像素。

非线性优化的结果依赖初始值：初始值太差时，算法可能收敛缓慢或落入局部最优。

因此，典型流程是：
![](../图片/Pasted%20image%2020260815020624.png)

## 13. 实际标定时的常见假设

为了减少未知参数数量，实际标定常作如下近似：

- 图像坐标轴垂直，即 skew 为 0。
- 像素是方形的，即 $\alpha=\beta$ 或二者关系已知。
- 主点 $u_0,v_0$ 接近图像中心，必要时直接设为图像中心。

这些假设会降低问题难度，也能使优化更稳定。但如果相机具有明显畸变、非方形像素或特殊成像结构，则应放松这些假设。

## 14. 本讲总结

相机标定的本质是：利用已知三维点和其二维图像点之间的对应关系，求解相机从三维到二维的映射。

线性阶段的核心是：

$$  
Pm=0  
$$

通过 SVD 得到相机矩阵 $M$ 的初始估计。

完整的相机矩阵可以分解为：

$$  
M=K  
\begin{bmatrix}  
R&T  
\end{bmatrix}  
$$

其中 $K$ 是内参，$R,T$ 是外参。

真实镜头存在径向畸变，因此最终通常需要最小化重投影误差：

$$  
\sum_i  
\left|  
p_i-\hat{p}_i(Q)  
\right|_2^2  
$$

相机标定是后续单视图测量、双目匹配、三维重建、AR、SLAM 和机器人视觉的基础。
